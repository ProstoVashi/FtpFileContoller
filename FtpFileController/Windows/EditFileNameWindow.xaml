<Window x:Class="FtpFileController.Windows.EditFileNameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:FtpFileController.Windows"
        mc:Ignorable="d"
        Title="Окно настроек имени файла" Height="450" Width="800"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        Closed="Window_Closed">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="1" Grid.Column="1" 
               Content="Введите имя файла:" 
               VerticalAlignment="Center" HorizontalAlignment="Center" 
               FontSize="24" FontWeight="Bold"/>
        <TextBox x:Name="FileNameTextBox"
            Grid.Row="1" Grid.Column="2" 
                 VerticalAlignment="Stretch" HorizontalAlignment="Stretch" TextAlignment="Left"
                 FontSize="20"/>
    </Grid>
</Window>
